import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { OpenBatingawPage } from './open-batingaw';

@NgModule({
  declarations: [
    OpenBatingawPage,
  ],
  imports: [
    IonicPageModule.forChild(OpenBatingawPage),
  ],
})
export class OpenBatingawPageModule {}
